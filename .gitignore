__pycache__/
*.pyc
.env
.venv/

# live runtime state (regenerated each session)
state.json

# secrets — never commit
helius_key.txt

# large live data streams (regenerated 24/7 by the tracker)
curves.jsonl
mfg_trades.jsonl
mfg_tokens.jsonl
mfg_state.json

# scratch
_tmp_*
_track.log
live_wallet.json
mfg_wallet_trades.jsonl
mfi_account_key.json
LIQ_FIRE_OK
mfg_liq_fires.jsonl

# 70MB rotating gPA state — too big for git
damm_v2_birth_state.json
damm_v2_births.jsonl

# dexscreener-derived rotating state
pumpswap_pairs.json
knots_txs.jsonl
gbr_*.jsonl

# large rotating data logs — keep local, don't push
bsc_flow.jsonl
base_flow.jsonl
bin_snapshots.jsonl
damm_v2_fee_snapshots.jsonl
knots_txs_pre.jsonl
node_modules/

# nested node_modules
**/node_modules/
